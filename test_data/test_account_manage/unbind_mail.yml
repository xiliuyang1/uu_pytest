-
  name: 邮箱解绑
  method: post
  url: /api/user/phone/unBind
  headers:
    token: "{{token()}}"
    Content-Type: application/x-www-form-urlencoded;charset=UTF-8
  datas:
    mail: "{{read_extract_yaml()['unbind_mail']}}"
    code: 8888
  verify:
    - eq: {"msg": OK}
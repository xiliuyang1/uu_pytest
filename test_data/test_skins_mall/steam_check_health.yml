-
  name: 饰品商城中，切换steam账号时的账号检测
  method: post
  url: /api/steam/store/checkHealth
  headers:
    Content-Type: application/x-www-form-urlencoded;charset=UTF-8
    token: "{{token()}}"
  datas:
    steam_id: "{{read_extract_yaml()['steam_id']}}"
    scene: 2
  verify:
    - eq: {"msg": OK}